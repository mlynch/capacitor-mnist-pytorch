import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-mnist-pytorch',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
